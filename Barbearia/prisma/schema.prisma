generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
}

model Agendamento {
  id        Int      @id @default(autoincrement())
  nome      String
  telefone  String
  servico   String
  data      DateTime
  criadoEm DateTime @default(now())
}